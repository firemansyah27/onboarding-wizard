import React from "react";
interface Props {
  submitRef4?: React.LegacyRef<HTMLButtonElement> | undefined;
}
const OnboardingStep4: React.FunctionComponent<Props> = ({ submitRef4 }) => {
  return <p>Step 4</p>;
};

export default OnboardingStep4;
